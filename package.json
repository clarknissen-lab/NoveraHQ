{
  "name": "novera-studio-os",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Novera Studio OS — baut den kompletten Notion-Workspace über die Notion-API.",
  "scripts": {
    "build": "node scripts/build-notion.mjs",
    "build:dry": "node scripts/build-notion.mjs --dry-run",
    "verify": "node scripts/verify.mjs"
  },
  "dependencies": {
    "@notionhq/client": "^5.26.0"
  },
  "engines": { "node": ">=18" }
}
