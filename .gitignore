node_modules/
.env

# Der Prüflauf schreibt hierhin — Mock-IDs gehören nie ins Repo.
.novera-state.verify.json
.DS_Store

# .novera-state.json ist bewusst NICHT ausgeschlossen: Die Datei enthält nur die
# IDs der angelegten Notion-Objekte, keine Zugangsdaten. Sie muss erhalten
# bleiben — ohne sie legt ein zweiter Lauf alles ein zweites Mal an.
